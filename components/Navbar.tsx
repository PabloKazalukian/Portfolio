import type { NextPage } from 'next';

const Navbar: NextPage = ( ) => {
    return(
        <nav>
            <ul>
                <li>inicio</li>
            </ul>
        </nav>
    )
}

export default Navbar;